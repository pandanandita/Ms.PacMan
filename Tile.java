//Nandita Panda//Feb 21, 2026//Project 3import java.awt.Graphics;import java.awt.image.BufferedImage;public class Tile {    private int x, y, w, h;    public static BufferedImage image = null;    public  Tile(int x, int y) {        this.x = x;        this.y = y;        this.w = 40;        this.h = 40;        load_image();   //tile image    }    // rebuilds tile object from json object    // used to load map from a file    //converts to json    public Tile(Json ob) {        this.x = (int)ob.getLong("x");        this.y = (int)ob.getLong("y");        this.w = (int)ob.getLong("w");        this.h = (int)ob.getLong("h");        load_image();       //ensures map is loaded    }    // converts current tile object into json    // saves tile's data to file    //converted from json    public Json marshal() {        Json ob = Json.newObject();        ob.add("x", this.x);        ob.add("y", this.y);        ob.add("w", this.w);        ob.add("h", this.h);        return ob;    }    // lazy loading tile    //image loaded only once instead of thousands of times for thousands of tiles    private void load_image() {        if (image == null) {            image = View.loadImage("images/tile1.jpg");        }    }    public void draw(Graphics g, int scrollPos) {        g.drawImage(image, x, y - scrollPos, w, h, null);    }    //returns true if exists at that location    public boolean isAt(int x, int y) {        return (this.x == x && this.y == y);    }    //potential debugging for object    @Override    public String toString() {            return "Tile (x,y) = (" + x + ", " + y + "), w = " + w + ", h = " + h;    }    public int getLeft() { return x; }    public int getRight() { return x + w; }    public int getTop() { return y; }    public int getBottom() { return y + h; }    public int getX() {        return x;    }    public int getY() {        return y;    }    public int getW() {        return w;    }    public int getH() {        return h;    }    public void setX(int x) {        this.x = x;    }    public void setY(int y) {        this.y = y;    }    public void setW(int w) {        this.w = w;    }    public void setH(int h) {        this.h = h;    }}